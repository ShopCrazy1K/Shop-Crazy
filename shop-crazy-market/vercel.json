{
  "installCommand": "npm install",
  "buildCommand": "prisma db push --accept-data-loss --skip-generate || true && prisma generate && next build",
  "framework": "nextjs",
  "outputDirectory": ".next"
}

